export class Student {
id: number;
firstName: string;
lastName: string;
cityName: string;
constructor(fist: string, last: string, city: string) {
 this.firstName = fist;
 this.lastName = last;
 this.cityName = city;
}
}